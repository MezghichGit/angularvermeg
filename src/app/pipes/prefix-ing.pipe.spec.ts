import { PrefixIngPipe } from './prefix-ing.pipe';

describe('PrefixIngPipe', () => {
  it('create an instance', () => {
    const pipe = new PrefixIngPipe();
    expect(pipe).toBeTruthy();
  });
});
